name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '26.7.0'
    - run: npm ci
      working-directory: ./app
    - name: Run lint
      run: npm run lint
      working-directory: ./app
    - name: Run tests
      run: npm test
      working-directory: ./app
